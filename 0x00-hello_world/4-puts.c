#include <stdio.h>

/**
 * main-entry point
 *
 * descrepition : A C programming that prints with puts function
 *
 * return : always retursn(0) success */

int main (void)
{		
  puts("\"Programming is like building a multilingual puzzle");
   return(0);
}
